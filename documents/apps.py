from django.apps import AppConfig

class DocumentsConfig(AppConfig):
    """Configure l'application Django « documents »."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "documents"
