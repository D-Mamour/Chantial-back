from django.apps import AppConfig

class FinancesConfig(AppConfig):
    """Configure l'application Django « finances »."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "finances"
