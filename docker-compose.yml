services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: chantial
      POSTGRES_USER: chantial
      POSTGRES_PASSWORD: chantial
    ports:
      - "5432:5432"
    volumes:
      - chantial_postgres:/var/lib/postgresql/data
volumes:
  chantial_postgres:
